{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Unscheduled Task",
  "properties": {
    "id": {
      "type": "string",
      "description": "This is the unique identifier for the task. It is a UUID"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "duration": {
      "type": [
        "null",
        "number"
      ],
      "description": "The duration of the task in hours"
    },
    "importance": {
      "oneOf": [
        { "const": 1 },
        { "const": 2 },
        { "const": 3 }
      ],
      "description": "The importance of the task. This is a number between 1 and 3"
    },
    "dueDate": {
      "format": "date-time",
      "type": "string",
      "description": "The date that the task is due. This will only be the day, not the time"
    },
    "difficulty": {
      "oneOf": [
        { "const": "easy" },
        { "const": "medium" },
        { "const": "hard" }
      ]
    }
  },
  "type": "object"
}
