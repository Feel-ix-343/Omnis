{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Schema for the user preferences that are taken into consideration for the autoscheduling algo",
  "properties": {
    "ordering": {
      "oneOf": [
        { "const": "easy" },
        { "const": "hard" }
      ]
    },
    "pomodoro": {
      "type": "boolean"
    },
    "maxTime": {
      "type": "integer",
      "minimum": 0
    },
    "durationTime": {
      "type": "integer",
      "minimum": 0,
      "description": "How long the user wants to work in the day. TODO: We should chnage this to: when does the user want to be finished by?"
    },
    "startTime": {
      "type": "number",
      "description": "The start time in hours (with decimal)"
    }
  }
}
