/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // typedRoutes: true
  },
  reactStrictMode: false,
  typescript: {
    ignoreBuildErrors: true
  }
}

module.exports = nextConfig
